# Pinned runtime dependencies for the Superloop billing-anomaly detector.
# Install with:  pip install -r requirements.txt
# Java 17+ required on PATH; Windows also needs winutils (see README).
pyspark==4.1.1
delta-spark==4.3.1
pandas==3.0.5
plotly==6.9.0
pytest==9.1.0
